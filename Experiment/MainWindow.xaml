<Window x:Class="Experiment.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Experiment"
        xmlns:grid="clr-namespace:Experiment.Models"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        xml:lang="fr-FR"
        Title="Naria Pre-Alpha" Height="450" Width="800">
    <Window.Resources>
        <!--<Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="16"></Setter>
        </Style>
        <Style x:Key="textBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="18"></Setter>
        </Style>-->
    </Window.Resources>
    <Grid Margin="5,5,0,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!--<local:Planner x:Name="actualPlanner" Grid.Row="1"></local:Planner>-->
        <local:Calendar Grid.Row="1"></local:Calendar>
    </Grid>
</Window>
