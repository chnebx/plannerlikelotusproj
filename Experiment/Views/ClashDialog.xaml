<Window x:Class="Experiment.Views.ClashDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Experiment.Views"
        xmlns:conv ="clr-namespace:Experiment.Converters"
        mc:Ignorable="d"
        Title="Clash" Height="300" Width="340">
    <Window.Resources>
        <conv:BrushToGradientConverter x:Key="BrushToGradientConverter"></conv:BrushToGradientConverter>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeaderTxt" Grid.Row="0" FontWeight="Bold" FontSize="22" HorizontalAlignment="Center"></TextBlock>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding ClashElements}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="5">
                        <Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#FFF3F3F3" Offset="0"/>
                                <GradientStop Color="#FFFDFDFD" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel Margin="0,0,0,10" Orientation="Horizontal" Width="200">
                            <Border Margin="0,0,20,0" Background="{Binding ColorFill, Converter={StaticResource BrushToGradientConverter}}" Width="40" Height="40" CornerRadius="10">
                            </Border>
                            <TextBlock FontWeight="Bold" FontSize="29" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Path=Name}"></TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"></StackPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <StackPanel Height="50" HorizontalAlignment="Center" Grid.Row="3" Orientation="Horizontal">
            <Button IsDefault="True" Click="CloseBtn_Click" Content="Retour" Width="140" ></Button>
        </StackPanel>
    </Grid>
</Window>
